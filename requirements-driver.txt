# Driver/orchestrator environment (NO torch_xla, NO JAX)
# This env launches Ray, prepares HF checkpoints, and runs the ES loop.
ray>=2.50.0
transformers>=4.30.0
torch==2.8.0           # CPU-only is fine
tensorboard>=2.20.0
numpy>=1.21.0
pandas>=2.3.0
psutil>=5.8.0
# Do NOT install torch_xla or libtpu here.
# vLLM and JAX are NOT required in the driver env (engines install them per-actor).
